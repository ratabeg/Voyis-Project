export const getAllImages = async (req, res) => {
//   const result = await UploadService.processUpload(req.file);
    // const result = await 
    return res.json({ message: "Send all images" });
};

